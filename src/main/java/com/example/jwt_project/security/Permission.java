package com.example.jwt_project.security;

// Simple enum of permissions. Add more as your app grows.
public enum Permission {
    STUDENTS_READ,
    STUDENTS_WRITE,
    TODOS_READ,
    TODOS_CREATE,
    USERS_MANAGE
}
